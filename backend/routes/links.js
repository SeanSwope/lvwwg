const express = require("express");

const linksController = require("../controllers/links-controller");

const router = express.Router();

// router.get("/list", linksController.getUsersList);

module.exports = router;
